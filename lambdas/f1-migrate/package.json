{
  "name": "f1-update-drivers",
  "version": "1.0.0",
  "description": "Update local copy of driver standings",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm build/ -rf && tsc --build && tsc",
    "postbuild": "cp package.json build/package.json && cd build && npm install --only=production"
  },
  "author": "Brady Cassada",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.309.0",
    "@aws-sdk/lib-dynamodb": "^3.309.0",
    "@tsconfig/recommended": "^1.0.1",
    "@types/node": "^18.15.11",
    "@typescript-eslint/eslint-plugin": "^4.25.0",
    "@typescript-eslint/parser": "^4.25.0",
    "eslint": "^6.8.0",
    "prettier": "^1.9.1",
    "prettier-eslint": "^10.1.0",
    "typescript": "^4.6.2"
  }
}
